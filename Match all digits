import re

# Compile a pattern that matches digits
pattern = re.compile(r'\d+')

# Use it to find all matches in the string
text = "I have 2 apples and 10 bananas."
matches = pattern.findall(text)

print(matches)  # Output: ['2', '10']
